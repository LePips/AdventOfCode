struct Day2: Day {

    // 2*l*w + 2*w*h + 2*h*l
    func part1() -> CustomStringConvertible? {
//        let sides = sampleLines()
//            .map { $0.matches(for: "(\\d+)").asInts }
//
//        // type checker craps out when trying to do all compositional
//        return sides.map { sides in
//            [(sides[0] * sides[1]),
//            (sides[0] * sides[2]),
//            (sides[1] * sides[2])]
//        }
//        .map { $0 }
//        .sum()
        nil
    }

    func part2() -> CustomStringConvertible? {
        nil
    }
}
